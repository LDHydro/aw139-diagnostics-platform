# Nightly re-forecast: refreshes due dates against the latest utilisation and
# flags anything that has drifted towards a limit.
[Unit]
Description=ELP nightly maintenance re-forecast

[Timer]
OnCalendar=*-*-* 03:30:00
Persistent=true

[Install]
WantedBy=timers.target
